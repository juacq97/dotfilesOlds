#!/bin/sh

icon=󰮯 
ifg=^c#fabd2f^
tfg=^d^

if ! updates=$(checkupdates 2> /dev/null | wc -l ); then
    updates=0
fi


if [ "$updates" -gt 0 ]; then
    echo "$ifg $icon $tfg$updates"
else
    echo "$ifg $icon $tfg 0"
fi
